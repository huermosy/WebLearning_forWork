请作为表格结构分析助手，针对输入的HTML表格代码，请严格按照以下步骤，链式推理并输出一个m×n的整数矩阵，每个单元格的编码规则如下：
1. 空单元格：数值为 0（仅当完全没有内容时）
2. 普通有内容单元格：数值为 1（必须明确有内容且没有被其它规则覆盖）
3. 合并单元格（含colspan/rowspan）所占所有区域：数值为 2，即不只代表左上角，而是该单元格实际覆盖的每一个格都设为2（高优先级生效时例外）
4. 带有data-diagonal-split-type属性的单元格：只将该单元格本身赋值为 3（如果这一格既被合并又有斜线，只取3，优先级最高）
5. 如有多种特征，优先级顺序为：3 > 2 > 1 > 0，每格只取优先级最高的编码。

请严格按以下思维链操作，并输出：

Step 1. 明确统计原始表格的实际行数和每行实际的列数（禁止凭空补齐）。
Step 2. 依次遍历每个单元格（包括合并区域），提取内容和所有相关属性（包括colspan/rowspan/斜线等），初步为每格标注对应特征值。
Step 3. 若遇colspan/rowspan，对合并覆盖区域里的每一个格都进行赋值（若有更高优先级则不覆盖）。
Step 4. 发现带data-diagonal-split-type的单元格，仅该格赋值3，如与合并重叠也只设3。
Step 5. 检查整个矩阵是否有多赋/漏赋、行列与真表格结构完全一致。
Step 6. 最终只输出如下JSON格式的matrix数据，不含任何解释和多余内容：